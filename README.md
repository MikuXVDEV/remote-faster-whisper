Для получение полной справки. загляните на оригинальный репозиторий.

Проверялся на python3.10

python -m venv venv && . venv/bin/activate && pip install -r requirements.txt && python remote-faster-whisper.py
